---
description: After Unity script/asset edits, wait for natural compile+reload then check Console
alwaysApply: true
---

# Unity Console Check After Edits

When editing Unity C# scripts, shaders, ScriptableObjects, or other assets that trigger recompile:

1. Finish the code/asset changes first.
2. **Do not** call `refresh_unity` with `mode: force` or `compile: request` unless the user explicitly asks. Let Unity pick up file changes on its own.
3. **Do not** spam MCP tools while compile / domain reload is in progress.
4. Wait until Unity has **stopped compiling and finished domain reload**:
   - Poll `mcpforunity://editor/state` until `compilation.is_compiling == false`, `is_domain_reload_pending == false`, and tools are ready.
   - Domain reloads in this project can take 10–20+ seconds — be patient.
5. Then run `read_console` focused on **errors** (include warnings if relevant).
6. If new compile errors appear from the change, fix them and repeat steps 4–5.

Never claim an edit is done while Unity is still compiling or reloading.
